import { ScrollView } from 'react-native'

import { Container } from '../components/Container/styles'

const Bag: React.FC = () => {
  return (
    <ScrollView>
      <Container></Container>
    </ScrollView>
  )
}

export default Bag
