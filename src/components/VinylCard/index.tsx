import { View, Text } from 'react-native'

import { IVinyl } from '../../common/types'

const VinylCard: React.FC<IVinyl> = (props) => {
  return (
    <View>
      <Text></Text>
    </View>
  )
}

export default VinylCard
